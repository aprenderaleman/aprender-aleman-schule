node_modules/
dist/
.env
.env.local
.env.production
.DS_Store
*.log
.claude/
deploy/env.production

# Capacitor / native builds
# The `android/` and `ios/` folders are checked in (they hold app id,
# icons, permissions, plugin config) but their build outputs are not.
android/.gradle/
android/local.properties
android/app/build/
android/build/
android/.idea/
android/app/release/
android/keystore.jks
ios/App/build/
ios/App/Pods/
ios/App/Podfile.lock
ios/DerivedData/
ios/.xcode.env.local
ios/App/App.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
